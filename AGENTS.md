# Project agent instructions

## UI 변경 시 실제 화면 검증 (필수)

렌더러 UI의 모양이나 상호작용을 변경한 작업은 코드 검사와 컴포넌트 테스트만으로 완료 처리하지 않는다.

1. 실제 Chromium/Electron 렌더러에서 변경된 화면을 연다.
2. 일반 앱 흐름으로 해당 화면에 들어가기 어렵다면, 실제 프로덕션 컴포넌트와 스타일을 사용하는 임시 QA 엔트리/하네스를 만든다. 검증만을 위해 프로덕션 동작을 바꾸지는 않는다.
3. 주요 컨트롤을 실제로 조작해 대표 상태를 만든다. 입력, 선택, 드롭다운, 로딩/결과, 초기화, 직접 입력 유지 등 변경 범위의 핵심 흐름을 확인한다.
4. 대표 상태의 스크린샷을 캡처하고 이미지로 직접 확인한다. 잘림, 겹침, 가로 오버플로, 정렬, 간격, 읽기 어려운 텍스트가 없는지 본다. 반응형 레이아웃에 영향이 있으면 좁은 화면도 확인한다.
5. DOM/상호작용 검증 결과와 스크린샷 검토를 자동 테스트 및 빌드 검사와 함께 사용한다. 실제 화면을 열지 못했다면 열어 보았다고 보고하지 않는다.
6. 검증 후 임시 QA 소스, 서버/프로세스, 저장소 안의 스크린샷 산출물을 제거한다. 사용자 소유 파일이나 기존 변경은 건드리지 않는다.

새 채팅에서 작업하더라도 이 저장소의 UI 변경에는 위 절차를 동일하게 적용한다.
