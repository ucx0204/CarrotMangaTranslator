# Bundle 6 sound-effect checkpoint - 2026-09-19

Status: IN PROGRESS. Baseline: `f6257058`.
Continue on `feat/mcp-app-bridge` in the existing MCP-Review worktree only.
Bundles 1-5 are implemented with automatic checks; all live acceptance remains
in the final integrated queue. Do not restart the live app or touch user artwork,
authentication, model assets, master or releases.

## Work in this bundle

Reuse native sound-effect review geometry and immutable detector records; expose
candidate inspection and explicit review decisions separately from OCR/translation.
Reuse bundle-three selected analysis/text application and bundle-four selected
erasure rather than automatically combining those stages. Connect independent
app image-layer generation from saved, approved sound-effect text and existing
geometry, retaining native source-reference, transparency and redaction rules.
Use existing page ownership, context/version evidence, batch actions and exact
recovery. External generated images continue through bundle-five validated input.
Never treat the current chat model as an API the server can invoke implicitly.

Generation must explicitly permit external processing and require the configured
supported app provider/model. No hidden OCR, translation, erasure, automatic font
analysis or geometry replanning. Separate generated observations from application;
keep failures and successful results distinguishable, protect normal dialogue,
and preserve subsequent edits during undo/redo. Retain image-generation safety
refusals rather than clearing or routing around them.

## Resume

Inspect native sound-effect candidate/draft mutation and generation APIs, add
strict contracts and independent execution/adapters, then register tools and
behavioral tests. Record exact verified commits, automatic results and remaining
work before reporting completion. No bundle-six implementation is complete yet.
