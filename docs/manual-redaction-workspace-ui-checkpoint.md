# 수동 가리기 UI 체크포인트

## 사용자 수정 지시 — 2026-09-10

- 자동 감지·자동 위치 추천은 계속 제외한다.
- 상시 설명문은 최소화한다. 상태·오류·명시적 동의는 짧게 표시하고, 보조 안내는 기존 툴팁으로 제공한다.
- `npm run dev`의 `manualRedaction` 리소스 누락부터 고친다. 타입 단언이나 언어 fallback으로 검사를 우회하지 않는다.

## 확인한 실패

- 이전 검사 소스 `9f824566`, 실행 `34450855346`: 중국어 간체/번체 키 누락, 페이지 필터의 string 타입 오류.
- 집중 테스트 37개와 mock 경계는 통과했지만 타입 검사와 신규 UI ESLint는 실패했다. 완료 상태가 아니다.
- 일부 새 함수의 크기·ref 사용·hook 의존성도 정리해야 한다. 기준을 낮추거나 lint disable을 추가하지 않는다.

## 이번 수정

- 다섯 언어의 실제 번역과 동일한 키/보간 변수 검증을 추가한다.
- 이전 3개 언어만 작성하던 체크포인트 스크립트를 교체한다.
- `npm run dev`와 동일한 `tsconfig.electron.json` 컴파일 및 실제 build를 검증에 포함한다.
- 화면 설명을 제거하고 필수 안내는 툴팁으로 옮기는 작업을 진행한다.

## 재개

`docs/manual-redaction-latest-validation.md`의 소스 SHA와 실행을 확인하고, 실패 항목부터 이어서 수정한다. 3차의 사전 편집 진입점·전체 Check·실제 UI QA는 아직 완료로 표시하지 않는다. master·태그·릴리즈는 수정하지 않는다.
