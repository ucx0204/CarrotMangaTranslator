# 수동 가리기 최신 검증과 재개 위치

브랜치: `feat/manual-redaction-workspace-20260910`
최신 제품 커밋: `e67713d2875dc891e92321f1c4c1f1a7edb8b1c0`
이 제품 커밋의 Windows 검사: `34508336718` (이 기록 시점에는 실행 중)

## 원격에 보존된 수정

이전 ZIP을 다시 적용하지 않는다. 미리보기 미표시 페이지의 추가 확인 체크박스 제거, 제한된 모달 너비, 숫자 입력과 인접 버튼의 겹침 수정, 왼쪽 선택 도구 정리는 이미 원격 코드에 있다. 실제 이미지 오류와 최종 전송 승인은 계속 검사한다.

- `9fb9913`: 선택 도구와 고급 메뉴를 분리해 린트와 포맷 오류 수정.
- `348dd3a`: 실제 화면 QA의 잘못된 번역 키 수정. 생성된 QA 엔트리도 Chromium 실행 전에 타입 검사한다.
- `4d9ea62`: Enter 후 캔버스 포커스 유지. 페이지 번호는 입력 도중 이동하지 않고 Enter/포커스 해제 때 확정한다. 두 자리 페이지 번호 회귀 추가.
- `63492c6`: IPC의 기존 데이터 루트와 디코더를 세션/미리보기에 주입. 원본 스냅샷, 지문, 취소, 저장 격리 회귀 추가.
- `e67713d`: 닫힌 화면의 비동기 오류 갱신을 차단하는 공통 오류 상태와 기존 웹 가져오기 회귀. 중복된 일괄 확인 푸터 컨테이너 제거. 구조 검사 예산은 올리지 않았다.

## 완료한 검사와 범위

제품 `4d9ea623b05d241809181f97087a9ba1590f61b1`, Windows Actions `34506762104`:

- TypeScript, Electron TypeScript, 실제 개발 main 컴파일, 집중 ESLint, mock 경계, 전체 앱 build 통과.
- 12개 파일의 테스트 74개 통과.
- 실제 프로덕션 컴포넌트 화면 검사 11개 통과. 1600×980, 1240×760, 960×620, 확인 전/후, 전체 확인, 선택 메뉴, 브러시, 일괄 확인, 1000장, 영어/일본어를 포함한다.
- PNG를 직접 열어 숫자 필드, 페이지 이동/확인 상태, 선택 도구, 메뉴 잘림과 겹침을 확인했다. 일괄 확인창에는 추가 체크박스가 없다.
- 이 소스의 전체 check에서는 구조 검사 문제가 남았으며, 위 두 후속 제품 커밋에서 수정했다. 해당 실행을 전체 성공으로 표시하지 않는다.

제품 `e67713d`, 로컬 Linux/Node 22:

타입 검사, Electron 타입 검사, 린트, 포맷, 오류 처리, mock 경계, 구조, 유지보수 규칙, 중복, 재노출, 생성 파일, CSS 구조, 스크립트 진입점 검사가 통과했다. 신규 디코더/저장 격리 테스트 4개와 UI/공통 오류/웹 가져오기 테스트 19개가 통과했다. 로컬 전체 check는 Knip의 네이티브 parser 메모리 할당 오류에서 멈췄다. 검사를 끄거나 기준을 낮추지 않고 완전한 Windows checkout에서 전체 검사를 실행한다.

## 재개

현재 원격 HEAD와 검사 소스를 대조하고 Actions `34508336718`의 최종 결과와 artifact부터 읽는다. UI 및 컴파일 성공과 전체 check 성공을 구분한다. 실패가 있으면 실제 로그를 기준으로 작은 커밋으로 수정한다. 모두 끝나면 이 문서에 최종 소스/실행/검사 결과를 기록한다.

이번 범위는 사용자가 보고한 체크박스/레이아웃/포커스/CI 수정이다. 원래 3차의 사전 편집 앱 진입점은 별도 미완료 작업이며 완료했다고 표현하지 않는다. 자동 감지, 기본 브랜치, 버전/태그/릴리즈, 사용자 원본/보관함/출력물은 변경하지 않았다.
