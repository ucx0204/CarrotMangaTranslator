# 수동 전송 이미지 가리기 작업 인계

## 작업 범위와 권한

- 사용자 요청: 2026-09-10, 계획의 1~3차를 새 브랜치에서 구현하고 중단 후에도 이어갈 수 있게 한다.
- 작업 브랜치: `feat/manual-redaction-workspace-20260910`
- 기준 커밋: `19b660f4160e4a64ce479caa09a20437f9ed3c81`
- **수동 전용. 자동 감지, 자동 마스킹, 자동 위치 추천은 앞으로도 이 작업에 추가하지 않는다.**
- `master` 병합, 버전 변경, 태그 생성, 앱/자산 릴리즈 게시 권한은 이번 요청에 포함되지 않는다.
- 원본 이미지·보관함·기존 출력물을 수정하거나 삭제하지 않는다. 가리기는 외부 전송 복사본에만 적용한다.

## 중단 후 재개 절차

1. 위 브랜치를 가져온 뒤 `AGENTS.md`, 본 문서, `git log -8 --oneline`, `git diff`를 먼저 확인한다. 같은 이름의 새 브랜치나 새 작업을 다시 만들지 않는다.
2. 이 문서의 체크포인트와 해당 커밋의 실제 diff를 대조한다. 체크되지 않은 항목을 완료로 간주하지 않는다.
3. 브랜치의 최신 Actions 실행과 로그를 읽는다. 실패는 실제 로그를 근거로 수정하고, 검사를 삭제하거나 임계치를 낮춰 통과시키지 않는다.
4. 미완료 수정은 작은 기능 단위 커밋으로 보존한다. 테스트/QA 상태와 다음 실행 명령을 매 체크포인트에 기록한다.
5. 다른 사람이 브랜치를 갱신했다면 먼저 변경을 읽고 fast-forward/rebase한다. 강제 push 금지.
6. 모든 기능과 검증이 끝나도 기본 브랜치와 릴리즈는 건드리지 않는다. 결과와 실제 미검증 범위를 보고한다.

## 구현 체크리스트

### 1차: 연속 작업과 안전한 상태

- [ ] 로드·사용자 검토·저장 상태를 분리한 공유 세션 모델
- [ ] 이전/다음, 확인 후 다음 미확인, 보류, 명확한 진행률/차단 사유
- [ ] Z/X·방향키·Enter·Undo/Redo·도구/이동/확대, 입력·IME·키 반복·modal scope 보호
- [ ] 도구 설정 유지, 페이지별 가림/이력/확대·위치 유지
- [ ] 로컬 초안 자동 저장, 실패 복구, 저장 후 나가기, 전송 승인과 분리

### 2차: 대량 검토와 수동 편집

- [ ] 전체 그리드/연속 편집 전환, 썸네일, 범위·다중 선택, 필터, 페이지 이동
- [ ] 명시적 범위 확인, 보류/실패 모아보기 (prefetch가 확인을 대신하지 않음)
- [ ] 선택 가림 이동·크기 조절·삭제 및 가림 지우개, 실제 전송 마스크 일치
- [ ] 이전 페이지 가림 가져오기, 범위 일괄 적용, 크기/비율 불일치 경고, 기본 추가/명시적 교체
- [ ] 프리셋과 페이지/일괄 Undo/Redo
- [ ] 제한된 이미지 캐시·인접 prefetch·대량 목록 가상화·요청 race 방지

### 3차: 사전 편집과 이어하기

- [ ] 작업 전 작품/화/선택 페이지 준비 진입점 (공통 명령·gateway)
- [ ] 같은 원본/가림 버전의 검토 결과 재사용, 변경된 페이지만 재검토
- [ ] 중단 위치·선택/보류 상태 복원, 원본 변경 검증, 새 전송 세션에서 명시적 최종 승인
- [ ] 대량/반복/오류/취소 흐름 테스트와 넓은/좁은 실제 컴포넌트 UI QA

## 검증 계획

- 기존 이미지 가리기 픽셀/전송/원본 변경/취소/저장 실패 테스트 유지.
- 세션 reducer, 브러시/지우개 parity, 부분 편집, 일괄 취소, snapshot 검증, 안전한 저장 계약 단위 테스트 추가.
- UI: 100장 모두 수정/일부 수정/확인만, 미확인 점프, 단축키와 입력 충돌, 마지막 Enter 오전송 방지, 왕복 이력, 저장 실패/재시도, 빠른 페이지 전환.
- `npm run typecheck`, `npm run typecheck:electron`, lint/format/mock-boundary, 관련 Vitest, build 및 전체 `npm run check`.
- 저장소 자체 `qa:ui`로 실제 production component의 넓은/좁은 캡처와 overflow 검증. 정적 목업으로 대신하지 않는다.

## 현재 체크포인트

### 0 — 브랜치 생성 및 범위 고정

- 새 브랜치 생성 완료. 구현 시작 전 기록이다.
- 현재 기본 브랜치는 `19b660f`이고 이 브랜치도 같은 커밋에서 시작했다.
- 아직 제품 코드 변경이나 테스트 실행 없음.
- 다음: 현재 architecture/UI 규칙, IPC/gateway/command 진입점 확인 후 공유 세션·초안 저장 계약부터 구현.
- 이 실행 환경에서는 컨테이너의 GitHub DNS 접근이 실패한다. 연결된 GitHub 도구로 읽기/커밋하고 필요 시 브랜치 전용 Actions에서 검증한다. 토큰을 출력하거나 파일에 저장하지 않는다.
