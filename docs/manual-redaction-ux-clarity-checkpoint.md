# 수동 가리기 UX 명확화 — 작은 커밋으로 재개

브랜치: `feat/manual-redaction-workspace-20260910`
이번 통합 부모: `07ff90b288cd41a1e06d019b8e66f120f951925d`

## 실제 반영

- `8280d9c`: 선택 페이지 확인을 메뉴 밖으로 꺼내고 실제 UI 회귀를 수정했다.
- `f1c960d`: 기존 보류 초안을 미확인으로 읽되 가림, 확대, 위치, 선택, 저장 revision을 보존한다. 읽기만으로 저장하거나 전송하지 않는다.
- `4575ec6`, `07ff90b`: 동시에 반영된 5개 언어의 명확한 문구와 선택/썸네일 개선을 읽고 보존했다. 강제 push하지 않는다.
- 이 커밋: 보류 버튼, 단축키, 런타임 상태와 필터를 제거한다. 과거 보류 기록은 위 저장소 읽기 경계에서만 받아 미확인으로 변환한다.
- 현재 페이지 확인은 이미지 아래 페이지 탐색과 함께 둔다. 클릭은 해당 페이지만 확인하고 이동하지 않는다. 캔버스 Enter는 확인 후 다음 미확인으로 이동하되 마지막 페이지에서도 전송하지 않는다.
- 작업 전체의 실행은 별도 하단 `작업 재개` 버튼이다. 검토가 남거나 미리보기 오류가 있으면 전송하지 않는다. 준비 모드는 미확인 상태도 저장 후 닫을 수 있다.
- 진행률과 저장 상태는 클릭 영역/체크 아이콘이 아닌 텍스트다. 미확인 이동은 독립된 이름 있는 버튼이다.
- 선택 페이지 수를 확인 버튼에 표시하고 전체 선택/해제도 노출한다. 썸네일의 선택과 확인 여부는 서로 다른 텍스트로 표시한다. 기존 masked/error 선택 메뉴도 보존한다.

## 현재 검증 범위

Linux / Node 22.16.0 / lockfile 일치 의존성. 타입 검사와 실제 개발 main 컴파일 경로, 집중 ESLint가 통과했다. 10개 파일의 테스트 63개가 통과했고 동시 변경 통합 후 실제 연속 편집 UI 테스트 8개를 재실행해 통과했다.

`imageRedactionReview.test.ts`, `manualRedactionWorkspaceStore.test.ts` 두 추가 테스트 파일은 로컬 Electron 바이너리가 없어 수집 단계에서 실행되지 않았다. 테스트를 제거하거나 production 의존성을 가짜로 바꾸지 않았다. 이 결과는 전체 테스트 통과를 뜻하지 않는다.

아직 남음: 완전한 checkout의 Windows 검사/전체 build, 실제 넓은·좁은 화면 QA와 전체 check. 다음 검증은 이 원격 코드와 소스 SHA를 고정해서 수행한다. 원래 3차 앱 사전 편집 진입점은 이번 UX 수정의 완료 범위에 포함하지 않는다.

## 중단 후 재개

이 문서와 최신 HEAD, `AGENTS.md`, 실제 diff부터 확인한다. 이미 커밋한 코드를 처음부터 재작성하지 않는다. 새 수정은 작고 빌드 가능한 단위마다 커밋하고 원격 ref를 확인한다. 이전 체크포인트 기록은 당시의 범위이며 이 결과로 과거 미검증 사항이 자동 해결되는 것은 아니다.

자동 감지/추천 없음. master/버전/태그/릴리즈, 사용자 원본/보관함/기존 출력물은 변경하지 않는다.
