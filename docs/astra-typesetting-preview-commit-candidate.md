# v0.4.3 · Per-region preview results and exact artifact commit

Preregistered after discovery-004 candidate 3's persisted fourteen-page report
and exact original-render audit. This is candidate 4 of the same sealed chapter,
not fresh confirmation. Old page 6 remains locked at 1/10. No new manga run may
start before production checks and real server protocol preflight pass.

Observed mechanism: one invalid permission envelope aborts the entire preview
batch before any images/evidence are returned. The error omits the responsible
target. Candidate 3's final proposed r7 overlaps the protected star, while its
reason mistakenly attributes the problem to r8. No successful last-page preview
exists; the final generic binding error hides that distinction and aborts the
whole chapter after 505170 cumulative tokens. This is distinct from the localized
Japanese reading failure addressed by v0.4.2.

Return a result for each structurally valid requested target. Valid masks use
the unchanged exact production preview; an invalid/empty mask shows its original
crop, explicit target/protection diagnostics and unresolved status. Invalid
permission must never be applied or silently clipped. The candidate records both
the proposed geometry and the resolved artifact: unresolved regions have empty
permission and retain their source, with review required through the existing
bounded repair/fallback path. Unexpected input corruption or infrastructure
failures remain errors. The model still has at most three preview attempts.

The final model response references the last preview revision, SHA-256 and exact
unresolved region IDs, instead of transcribing polygons again. The runtime
resolves only that bound artifact into the existing erasure-plan domain contract.
Missing, stale, tampered or inconsistently acknowledged preview references fail.
This is explicit acceptance of a partial artifact, never an erasure quality pass.

The confirmed transformed-foreground protection defect is a separate mandatory
engineering correction in this candidate: reuse existing transform geometry in
pixel coordinates for keep conflicts and restoration closure on non-square
pages. Characterize unchanged ordinary behavior, rotation, warping and relevant
outline/overflow handling before changing authority. Preserve the initial plus
two downstream repair budget.

Required evidence: valid preview pixel parity, mixed valid/blocked requests,
region-specific diagnostics, no keep-pixel removal, empty masks, exact latest
revision/hash/membership binding, structural/abort/error behavior, actual strict
server commit response with image inspection, actual renderer transformed keep
protection, full checks/build, then the entire frozen chapter and native saved
image audit. Six unchanged model schemas retain only exact-hash-matched prior
preflight evidence. Do not claim commercial quality or cost savings from a schema
or synthetic test. Full chapter font grouping, preset, model and generation
mechanisms remain fixed; source data and old immutable reports remain intact.

Foreground protection implementation detail, fixed before the candidate run:
the production port additionally renders each proposed block alone on a native
transparent page and measures every nonzero alpha pixel. These actual painted
bounds, including curves, outlines, generated layers and custom-font overhang,
are passed into keep checking and restoration closure. Empty ink is explicitly
null; absent measurement is not the same thing. Production refuses a missing
transparent renderer or a rescaled capture. This adds local rendering work and
is not presented as a cost reduction. The existing transform helper supplies
pixel-space geometric fallback for older nonproduction ports without raster
evidence. Actual renderer overflow is also a deterministic repair issue even
when model review returns an empty issue list. No renderer algorithm is moved;
the transparent export API gains the same optional original-resolution capture
choice already supported by ordinary export, keeping its old default intact.

Preflight completed before any candidate 4 reservation: the real Astra high
server inspected two actual production preview returns and committed revision 2,
its exact hash and the unresolved r1 acknowledgement. Mixed valid/protected
geometry returns r1's unchanged source and r2's real cleaned preview. Applying
the resolved artifact leaves all 2500 r1 pixels exact and zero r2 target pixels.
Both rounds' eight native preview images and the final synthetic image were
directly viewed. The call used 18198 cumulative tokens; no ImageGen call. The
changed commit schema and unchanged preview input schema, plus six other output
schemas, have exact recorded hashes linked to actual server preflights.

Actual transparent rendering and restoration passed five synthetic transformed
foreground fixtures. Focused protocol, pixel, application continuation and export
tests passed, then all 26 repository check gates and build passed in 154.88 s
(`.tmp/astra-v043-check.log`). Artifacts are `preview-commit-preflight-v043/` and
`painted-protection-proof-v043/` under the research directory. The production UI
from v0.4.2 is unchanged; its actual wide/narrow captures and persisted localized
review marker proof still apply. No manga quality or cost improvement is claimed
from this preflight. The next trial remains candidate 4 on discovery-004.

Candidate 4 was reserved and started at 2026-09-05T22:56:11.390Z, trial
`dc344f04-3b12-4ae9-9c06-9e4398e3a2ed`, job
`56b8f43a-67f4-4acc-b967-9c2dff9d2456`. All fourteen sealed sources passed their
hash checks before import. The runtime/font inventory and fingerprint are frozen
in `discovery-004/candidate-004/candidate-contract.json`; no previous model
readings or answers are reused. This is 4/5 on the discovery unit, not a fresh
confirmation, and old page 6 remains at 1/10. No promotion during the running
trial. Native report and direct quality audit are still required at completion.
