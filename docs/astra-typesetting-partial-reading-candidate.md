# v0.4.2 · Localized reading failures

Preregistered after discovery-004 candidate 2's persisted report. Candidate 3
uses the same sealed fourteen-page discovery chapter; it is not fresh evidence.
Page 6 remains unused beyond its first locked trial.

The first page now reads correctly and its actual mask preview loop repairs a
cut balloon boundary. The second page instead contains a genuinely uncertain
localized Japanese SFX. The reader reports its location and the remaining text
but the page-wide gate discards everything. This newly observed failure differs
from native-image downscaling, clipped non-owned context and excluded English
readability. Those input/scope corrections remain necessary. Repeating or
loosening their prompts cannot implement the required regional failure behavior.

Use the playbook section 9's data-representation transition within the existing
five-candidate unit: represent localized unreadable Japanese separately from
readable translations and excluded decoration. Do not claim a partial result
is complete reading. Unlocalized/global unreadability remains an explicit error.
Contradictory status, absent reasons, duplicate IDs and invalid ownership are
rejected. No previous manga response is reused as a new candidate input.

Localized unreadable regions become protected keep regions with a specific
review reason, empty untranslated text and no invented source transcript. They
never enter erasure, source-font grouping, font assignment, image generation or
layout requests as active targets. Their source pixels remain protected by the
existing production mask contract. Final saved blocks expose needs_review and
preserved source geometry. An all-unreadable-but-localized page still saves
review markers and a warning, not an empty successful page.

Readable target translations must not overlap a protected region. Such placement
produces explicit text feedback within the existing initial-plus-two repair
budget; persistent conflicts restore the affected active region and overlap
closure. Keep content never becomes an active repair target. The global failure
gate is retained for cases without reliable localization.

Validate mixed readable/unreadable pages, unknown-only pages, later-view failure,
ownership edges, keep-pixel exclusion, overlap restoration, persistence/reopening
and exact original artwork beneath review markers. Use actual production UI for
the new saved review state. Preflight the changed read schema against the real
Astra server before any new manga run. Existing seven-stage v0.4.0 evidence only
applies to the six unchanged schemas. All upstream and downstream budgets,
fonts, erasure-preview/registration mechanisms and model remain unchanged.

Success at this boundary means remaining regions proceed and unresolved content
is visibly marked and preserved. It is not a commercial-quality promotion.
Count preserved/unreadable regions in the full-chapter denominator and report
their cost/status separately. Full native final-image audit and the existing
promotion/generalization requirements remain in force.
