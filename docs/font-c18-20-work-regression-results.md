# 승인 C18 — 기존 20작품 194페이지 재적용

2026-09-07. 사용자가 confirmation-011 앱 C18 결과를 승인한 뒤, 이전 약 200페이지에
같은 상태를 적용하도록 요청했다. 기존 20작품 194페이지 선택을 그대로 재사용했다.
새 일반화 검증이나 학습 데이터로 세지 않으며, 결과를 보고 C18을 다시 튜닝하지 않았다.

[원본 화질 A/B/C 비교 열기](http://127.0.0.1:8769/c18-20-work-002/review/index.html?page=W01%2FP001&view=C&zoom=fit&complete=1)

## 비교 범위

- A: 원본 파일의 이미지 bytes 그대로.
- B: v2.5.0 `5a0dcb7304a36d4034b06bebf31216eae7b333e4`의 typography와 exporter를 실제 실행.
- C: master `92fe6f230f492ccc1b5f8a28b07f34a7af9b97fb`의 승인 C18.1 앱 분석·적용·출력.

B/C는 고정 HayaiOCR 입력, 번역문, 지움 결과와 말풍선 기하를 공유한다. 폰트·굵기·크기 및
자연 줄바꿈 비교이며, 각 릴리스의 OCR부터 지움까지 전체 경로 비교는 아니다.
예전 보고서의 B/C 렌더를 재사용하지 않았다. C는 실제 앱 chapter prepare와 coordinator를
호출하며, 연구 폰트 선택값을 최종 block에 직접 주입하지 않았다.

표지와 긴 이미지는 기존 선택대로 제외했다. W04 긴 이미지 대신 W21이 들어간다.
W13은 7페이지, W17은 9페이지, W20은 8페이지이며 나머지는 각 10페이지다.
번역 대상이 없는 페이지도 누락시키지 않았다. 효과음 30영역은 C18 묶음 대상 밖이다.

## 완료 검증

| 항목                                          | 결과          |
| --------------------------------------------- | ------------- |
| 작품 / 페이지                                 | 20 / 194      |
| 원본 크기·해시 확인한 A/B/C 이미지            | 582개         |
| 작품별 독립 HTML에 내장한 이미지 bytes 대조   | 582개         |
| 전체 출력 영역 / C18 적용 대상                | 1,252 / 1,222 |
| C18 결정과 최종 서체·굵기·bold 불일치         | 0             |
| 묶음 안에서 동일 서체·굵기로 적용된 원문 그룹 | 443           |
| B/C 이미지가 다른 페이지                      | 181           |
| B/C 서체 또는 굵기가 다른 영역                | 1,137         |

변경 수는 개선 수가 아니며, 적용 일치는 폰트 정답률이나 원문 묶음 정확도가 아니다.
입력 662파일과 원본 파일의 해시, 두 버전·exporter·어댑터 해시 및 번역문 해시도 확인했다.

작품마다 대표 1페이지, 총 20페이지를 직접 열어 확인했다. 194페이지 전체를 시각적으로
감사했다고 주장하지 않는다. 일반 대사에서 안정적인 표본과 함께 다음 반례도 보존했다.

- W13/P001, W18/P001: 비정형 손글씨가 일반 고딕으로 선택된다. 반복 효과음 불일치도 남는다.
- W03/P001, W10/P001, W12/P001: 강한 명조·붓 느낌이 약해지는 부분이 있다.
- W15/P001: 연결 일반 대사의 계열·크기 차이가 남고 인물 소개의 둥근 굵은 서체가 과장된다.
- W17/P001, W16/P001: 작은 글자, 좁은 영역의 단어 중간 줄바꿈과 독립 문장부호가 남는다.

## 산출물과 재현

산출물 루트는
`C:/Users/sam40/Downloads/망가번역기-font-palette-lab-20260905/artifacts/font-palette-lab/c18-20-work-002`다.

- `review/index.html`: 작품/페이지 선택, A/B/C 한 장씩 전환, 100%·150%·200% 확대.
- `review/Wxx-embedded.html`: 원본 이미지 bytes를 내장한 작품별 독립 HTML 20개.
- `completion-receipt.json`: 전체 이미지·내장 bytes·적용·입력·실행 버전 검증 결과.
- `review/inventory.json`, `input-inventory.json`, `execution-seal.json`: 페이지와 입력·실행 권위.
- `visual-review.json`: 직접 본 20페이지와 잔여 문제. 평가 전용 관찰이며 human gold가 아니다.
- `qa/`: 최종 Chromium 1440×1000, 700×900 화면과 해시 기록. 잘림·겹침·외부 가로 스크롤을 확인했다.
- `reproduction/`: 실행·렌더·보고서·검증 스크립트 8개와 해시 목록.

재현 스크립트는 실행 코드 버전의 기본 워크트리 `.tmp`에 복사해 사용한다.
`c18-prepare-20.py`는 기존 캠페인을 덮어쓰지 않으며, 재실험에는 별도 산출물 경로가 필요하다.
`c18-20-complete.py`는 정확한 실행 HEAD에서 검증해야 한다. 추론 중 check/build를 병행하면
runtime이 재생성되므로 순차 실행한다. 원본 코퍼스와 기존 산출물은 보존한다.

Windows MIME 추측이 WebP를 인식하지 못해 독립 HTML 생성 한 번이 실패했다.
보고서의 명시적 MIME 매핑만 고쳤으며 완료된 추론·렌더는 재실행하지 않았다.
해당 실패 로그와 최초 실행 결과를 남겼다. 이후 모든 내장 bytes 검증을 통과했다.

제품 전체 검사는 [C18 앱 통합 인계서](font-chapter-c18-handoff.md)의 통과 기록을 따른다.
이 배치 중 제품 알고리즘·모델·매핑 변경은 없으며 공개 installer 릴리스는 하지 않았다.
