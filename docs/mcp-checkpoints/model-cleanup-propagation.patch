diff --git a/src/main/appActivityGate.ts b/src/main/appActivityGate.ts
--- a/src/main/appActivityGate.ts
+++ b/src/main/appActivityGate.ts
@@ -1,3 +1,4 @@
+import { assertModelCleanupComplete } from "./runtimeSupport/modelCleanupBarrier";
 import {
   activityResourcesConflict,
   activityConflictReason,
@@ -103,4 +104,5 @@
   ): void {
+    assertModelCleanupComplete(resources);
     if (this.permanentlyClosed || this.suspensionTokens.size > 0)
       throw new AppActivityClosedError();
     const conflict = this.findConflict(resources, ownerId);
@@ -177,4 +179,5 @@
   ): AppActivityLease {
+    assertModelCleanupComplete(input.resources);
     if (this.permanentlyClosed || this.suspensionTokens.size > 0) {
       throw new AppActivityClosedError();
     }
@@ -196,4 +199,5 @@
       updateResources: (resources) => {
         if (released) throw new Error("Activity lease is already released.");
+        assertModelCleanupComplete(resources);
         const conflict = this.findConflict(
           resources,
diff --git a/src/main/inpainting/fluxEnginePool.ts b/src/main/inpainting/fluxEnginePool.ts
--- a/src/main/inpainting/fluxEnginePool.ts
+++ b/src/main/inpainting/fluxEnginePool.ts
@@ -1,3 +1,4 @@
+import { releaseModelResource } from "../runtimeSupport/modelCleanupBarrier";
 import { join } from "node:path";
 import type { AppPaths } from "../appPaths";
 import {
@@ -122,4 +123,4 @@
 ): Promise<void> {
   try {
-    await engine.dispose();
+    await releaseModelResource(engine, () => engine.dispose());
     logInpaintingRuntimeInfo("Flux inpainting engine disposed", { reason });
@@ -133,5 +134,6 @@
         error,
       },
     );
+    throw error;
   }
 }
diff --git a/src/main/inpainting/koharuEnginePool.ts b/src/main/inpainting/koharuEnginePool.ts
--- a/src/main/inpainting/koharuEnginePool.ts
+++ b/src/main/inpainting/koharuEnginePool.ts
@@ -1,3 +1,4 @@
+import { ModelCleanupError, releaseModelResource } from "../runtimeSupport/modelCleanupBarrier";
 import { join } from "node:path";
 import type { AppPaths } from "../appPaths";
 import { detectBestGpuInfo } from "../gpuInfo";
@@ -99,3 +100,4 @@
     } catch (error) {
+      if (error instanceof ModelCleanupError) throw error;
       errors.push(
         `${backend}: ${error instanceof Error ? error.message : String(error)}`,
@@ -187,4 +189,4 @@
 ): Promise<void> {
   try {
-    await engine.dispose();
+    await releaseModelResource(engine, () => engine.dispose());
     logInpaintingRuntimeInfo("Koharu inpainting engine disposed", { reason });
@@ -198,5 +200,6 @@
         error,
       },
     );
+    throw error;
   }
 }
diff --git a/src/main/textDetection/animeTextDetectorPool.ts b/src/main/textDetection/animeTextDetectorPool.ts
--- a/src/main/textDetection/animeTextDetectorPool.ts
+++ b/src/main/textDetection/animeTextDetectorPool.ts
@@ -1,3 +1,4 @@
+import { releaseModelResource } from "../runtimeSupport/modelCleanupBarrier";
 import { AbortableExclusiveGate } from "../runtimeSupport/abortableExclusiveGate";
 import { LeasedIdleResourcePool } from "../runtimeSupport/leasedIdleResource";
 import { prepareAnimeTextWorkerLaunch } from "./animeTextAssets";
@@ -49,4 +50,4 @@
 ): Promise<void> {
   try {
-    await worker.dispose();
+    await releaseModelResource(worker, () => worker.dispose());
     logAnimeTextInfo("anime-text-yolo worker disposed", { reason });
@@ -56,5 +57,6 @@
       reason,
       error,
     });
+    throw error;
   }
 }
