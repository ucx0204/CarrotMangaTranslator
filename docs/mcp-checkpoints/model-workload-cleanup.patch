diff --git a/src/main/translationRuntime.ts b/src/main/translationRuntime.ts
--- a/src/main/translationRuntime.ts
+++ b/src/main/translationRuntime.ts
@@ -70,12 +70,16 @@
 export async function disposeTranslationRuntimeResources(
   reason: string,
 ): Promise<boolean> {
-  const [groupingEvidenceDisposed, regionDetectorDisposed] = await Promise.all([
+  const settled = await Promise.allSettled([
     runtimeResources?.groupingEvidence.releaseIdleResources(reason) ??
       Promise.resolve(false),
     disposeCachedKoharuLayoutSessions(),
   ]);
-  return groupingEvidenceDisposed || regionDetectorDisposed;
+  const errors = settled.flatMap((result) =>
+    result.status === "rejected" ? [result.reason] : [],
+  );
+  if (errors.length) throw new AggregateError(errors, "OCR model cleanup did not complete.");
+  return settled.some((result) => result.status === "fulfilled" && result.value);
 }
diff --git a/src/main/mcp/mcpOcrAdapter.ts b/src/main/mcp/mcpOcrAdapter.ts
--- a/src/main/mcp/mcpOcrAdapter.ts
+++ b/src/main/mcp/mcpOcrAdapter.ts
@@ -49,5 +49,6 @@
     };
   } finally {
+    operation.progress({ phase: "releasing_model" });
     await disposeTranslationRuntimeResources("mcp-ocr-finished");
   }
 }
diff --git a/src/main/inpainting/fluxEngine.ts b/src/main/inpainting/fluxEngine.ts
--- a/src/main/inpainting/fluxEngine.ts
+++ b/src/main/inpainting/fluxEngine.ts
@@ -1,3 +1,2 @@
 import { basename, dirname, join, resolve } from "node:path";
-import { safeCleanup } from "../safeCleanup";
 import { FluxWorker, type FluxWorkerDiagnostics } from "./fluxWorker";
@@ -47,7 +46,4 @@
     if (worker && !worker.isHealthy()) {
-      void safeCleanup("dispose unhealthy Flux worker", () =>
-        worker?.dispose(),
-      );
-      worker = null;
+      throw new Error("Flux worker is unhealthy; finish native cleanup before retrying the workload.");
     }
     worker ??= new FluxWorker(options.launch, {
diff --git a/src/main/inpainting/koharuEngine.ts b/src/main/inpainting/koharuEngine.ts
--- a/src/main/inpainting/koharuEngine.ts
+++ b/src/main/inpainting/koharuEngine.ts
@@ -1,2 +1,1 @@
-import { safeCleanup } from "../safeCleanup";
 import type {
@@ -96,7 +95,4 @@
     if (worker && !worker.isHealthy()) {
-      void safeCleanup("dispose unhealthy Koharu worker", () =>
-        worker?.dispose(),
-      );
-      worker = null;
+      throw new Error("Koharu worker is unhealthy; finish native cleanup before retrying the workload.");
     }
     worker ??= new KoharuWorker(options.launch);
diff --git a/src/main/inpainting/koharuEnginePool.ts b/src/main/inpainting/koharuEnginePool.ts
--- a/src/main/inpainting/koharuEnginePool.ts
+++ b/src/main/inpainting/koharuEnginePool.ts
@@ -147,10 +147,9 @@
   } catch (error) {
     if (engine) {
-      await engine.dispose().catch((disposeError) => {
-        logInpaintingRuntimeError("Failed to dispose failed Koharu engine", {
-          backend,
-          disposeError,
-        });
-      });
+      const failedEngine = engine;
+      await releaseModelResource(failedEngine, () => failedEngine.dispose());
     }
     throw error;
   }
 }
